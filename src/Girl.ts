class Girl extends Character
{
	constructor(m:GameMap, path:string, indexH:number, indexW:number,)
	{
		super(m, path, indexH, indexW, false, []);
	}
	public wakeup(checkPoint:MapNode[])
	{
		
	}
	public move()
	{
		
	}
    // constructor(m : GameMap, path : string, indexH : number, indexW : number, blockable : boolean, checkPoints : MapNode[]){
}